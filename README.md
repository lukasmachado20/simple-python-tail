# Simple Tail Project

This project is used for study only. It's a simple implementation of a `tail` program that read the last lines of a file in linux.

This example uses the concepts of Generators and Iterators in Python.
